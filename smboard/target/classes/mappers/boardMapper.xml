<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.smallman.board.mappers.boardMapper">

	<select id="listPageCriteria" resultType="org.smallman.board.vo.BoardVO">
		select *
		from tbl_board
		where b_num > 0
		order by b_num desc
		limit #{pageStart}, #{perPageNum}
	</select>
	
	<select id="totalCount" resultType="java.lang.Integer">
		select count(*)
		from
		tbl_board
	</select>
	
	<select id="boardRead" resultType="org.smallman.board.vo.BoardVO">
		select *
		from tbl_board
		where b_num = #{b_num}
	</select>


</mapper>